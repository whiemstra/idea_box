require 'rails_helper'

RSpec.describe User, type: :model do
  it 'has a username attribute'

  it 'requires a username to be present'
  it 'requires a username to be unique'

  it 'requires a password to be set'
end
